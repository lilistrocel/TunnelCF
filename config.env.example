# Cloudflare Tunnel Configuration
# Copy this file to /etc/cf-tunnel/config.env and fill in your values

# ============================================================================
# Required Settings
# ============================================================================

# Cloudflare API Token with permissions:
# - Account > Cloudflare Tunnel > Edit
# - Zone > DNS > Edit
CF_API_TOKEN="your-api-token-here"

# Your Cloudflare Account ID (found in dashboard URL or domain overview)
CF_ACCOUNT_ID="your-account-id-here"

# Zone ID for your domain (found in domain overview)
CF_ZONE_ID="your-zone-id-here"

# Your domain name (must be added to Cloudflare)
CF_DOMAIN="yourdomain.com"

# ============================================================================
# Optional Settings
# ============================================================================

# Prefix for tunnel names (default: node)
# Final hostname will be: {NODE_PREFIX}-{machine_id}.{CF_DOMAIN}
# Example: node-abc123def456.yourdomain.com
NODE_PREFIX="rpi"

# Local SSH port (default: 22)
SSH_PORT="22"

# Additional services to expose through the tunnel
# Format: "subdomain1.domain.com:service1,subdomain2.domain.com:service2"
# Examples:
#   - Web server: "web-{machine_id}.yourdomain.com:http://localhost:80"
#   - API: "api-{machine_id}.yourdomain.com:http://localhost:8080"
#   - VNC: "vnc-{machine_id}.yourdomain.com:tcp://localhost:5900"
# Note: {machine_id} is NOT auto-replaced here, use actual values or leave empty
ADDITIONAL_SERVICES=""
